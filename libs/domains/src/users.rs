//! # Users

/// Service
// pub mod service;

/// Model
pub mod model;

/// GraphQL Mutations
// pub mod mutations;

/// GraphQL Resolver
pub mod resolver;

// Authorization rules - TODO if we use these they should have three slashes instead of two. just two now to avoid errors.
// pub const AUTHORIZATION: &str = include_str!("users/authorization.polar");

// Tests - TODO if we use these they should have three slashes instead of two. just two now to avoid errors.
// #[cfg(test)]
// mod tests;
